# Hotfix — OntologiaService unclosed comment

Erro: unclosed comment / reached end of file while parsing
Causa: JavaDoc cortado ao extrair OntologiaFichaService.

```bash
unzip -o magistratura-hotfix-ontologia-service.zip
mvn spring-boot:run
```
